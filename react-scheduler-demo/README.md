# React 调度器与浏览器事件循环演示项目

这是一个交互式演示项目，深入讲解 React 工作流程与浏览器事件循环的关系，重点解答：**"浏览器中的任务没有优先级，但是 React 中可以通过 Fiber 自己的调度器来调度任务，这是为什么？"**

## 🎯 项目目标

通过交互式演示帮助你理解：

1. **浏览器事件循环的工作机制**
   - 宏任务、微任务的执行顺序
   - 渲染时机
   - 任务队列的 FIFO 特性

2. **React 时间切片（Time Slicing）原理**
   - 如何将大任务拆分成小任务
   - MessageChannel 的使用
   - 保持 UI 响应性的技巧

3. **React 优先级调度机制**
   - 5 个优先级级别
   - 优先级如何影响执行顺序
   - useTransition 的实际应用

4. **React Scheduler 的实现原理**
   - 基于过期时间的调度
   - 防止任务饥饿
   - 可中断与恢复机制

5. **React vs 浏览器深度对比**
   - 为什么浏览器没有优先级
   - React 如何实现优先级调度
   - Fiber 架构的作用

## 🔑 核心问题解答

### 为什么浏览器任务没有优先级，但 React 可以实现优先级调度？

**简短回答：**

浏览器的事件循环确实没有提供任务优先级的 API（除了宏任务和微任务的区别）。但 React 通过**时间切片（Time Slicing）**和**自己的调度器（Scheduler）**，将一个大的更新任务拆分成多个小任务，然后根据优先级决定执行顺序。

**详细解释：**

1. **浏览器层面：**
   - 浏览器只区分宏任务（setTimeout, 事件回调等）和微任务（Promise, MutationObserver）
   - 同类型的任务按照 FIFO（先进先出）队列执行，没有优先级概念
   - 你无法告诉浏览器"这个 setTimeout 比那个 setTimeout 更重要"

2. **React 的解决方案：**
   - **不依赖浏览器的优先级**：React 自己管理所有任务
   - **时间切片**：把大任务拆成小任务，每个小任务执行完都会检查是否需要让出主线程
   - **优先级队列**：React 维护自己的任务队列，根据优先级排序
   - **可中断与恢复**：低优先级任务执行时，如果来了高优先级任务，可以中断并稍后恢复

3. **实现机制：**
   - 使用 `MessageChannel` 或 `setTimeout` 创建宏任务
   - 每个宏任务执行一小段工作（5ms 左右）
   - 在宏任务之间检查是否有更高优先级的任务需要执行
   - 如果有，先执行高优先级任务，低优先级任务稍后继续

**类比理解：**

想象浏览器是一个只有一个窗口的银行，所有人排队办理业务（FIFO）。React 相当于派了一个"大堂经理"，他可以：

- 把每个客户的业务拆分成多个小步骤
- 执行一小步后，看看队伍里有没有"VIP客户"（高优先级）
- 如果有 VIP，暂停当前客户，先服务 VIP
- VIP 服务完后，继续之前的客户

**浏览器只提供了"窗口"（事件循环），React 提供了"大堂经理"（调度器）。**

## 📁 项目结构

```
react-scheduler-demo/
├── src/
│   ├── demos/
│   │   ├── Home.tsx                      # 首页和核心问题解答
│   │   ├── EventLoopBasics.tsx           # 浏览器事件循环基础
│   │   ├── TimeSlicingDemo.tsx           # 时间切片演示
│   │   ├── PrioritySchedulingDemo.tsx    # 优先级调度演示
│   │   ├── SchedulerSimulation.tsx       # Scheduler 模拟实现
│   │   └── ReactVsBrowser.tsx            # React vs 浏览器对比
│   ├── App.tsx                           # 主应用和路由
│   └── App.css                           # 全局样式
└── README.md
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm start
```

然后在浏览器中打开 [http://localhost:3000](http://localhost:3000)

### 构建生产版本

```bash
npm run build
```

## 📚 演示内容

### 1. 首页（Home）

- 项目介绍和学习目标
- 核心问题的完整解答
- React 调度器的优势说明

### 2. 事件循环基础（EventLoopBasics）

**学习内容：**
- 宏任务 vs 微任务
- 事件循环的执行顺序
- 长任务如何阻塞 UI
- 浏览器为什么没有任务优先级

**交互演示：**
- 基础的宏任务、微任务执行顺序
- 多个任务的执行时机
- 包含渲染的事件循环
- 长任务阻塞演示（3秒）

### 3. 时间切片（TimeSlicingDemo）

**学习内容：**
- 什么是时间切片
- 为什么需要时间切片
- MessageChannel vs setTimeout
- React 的 5ms 时间片策略

**交互演示：**
- ❌ 阻塞方式：一次性执行 1000 个任务（卡顿）
- ✅ 时间切片方式：分批执行任务（流畅）
- 🎯 React 风格：使用 MessageChannel 实现

### 4. 优先级调度（PrioritySchedulingDemo）

**学习内容：**
- React 的 5 个优先级级别
- 优先级如何影响执行顺序
- React 18 的 useTransition API
- 用户交互优先的设计理念

**交互演示：**
- 手动添加不同优先级的任务
- React 风格执行（按优先级）vs 浏览器风格执行（FIFO）
- useTransition 实时搜索演示

### 5. 调度器模拟（SchedulerSimulation）

**学习内容：**
- React Scheduler 的实现原理
- 基于过期时间而非优先级值
- 如何防止任务"饥饿"
- workLoop 的工作机制

**交互演示：**
- 场景 1：基础优先级调度
- 场景 2：任务饥饿问题
- 场景 3：过期时间提升机制
- 手动构建任务队列并观察执行

### 6. React vs 浏览器（ReactVsBrowser）

**学习内容：**
- 全面对比浏览器和 React 的调度机制
- 深入解析核心问题
- 形象比喻帮助理解
- 代码示例对比

**内容包括：**
- 详细的对比表格
- 浏览器 vs React 代码对比
- 快餐店 vs 高级餐厅比喻
- 完整的总结和推荐阅读

## 💡 关键概念

### 浏览器事件循环

```javascript
while (true) {
  // 1. 执行一个宏任务
  const macroTask = macroTaskQueue.shift();
  execute(macroTask);
  
  // 2. 执行所有微任务
  while (microTaskQueue.length > 0) {
    const microTask = microTaskQueue.shift();
    execute(microTask);
  }
  
  // 3. 可能进行渲染
  if (shouldRender()) {
    render();
  }
}
```

### React Scheduler

```javascript
function scheduleCallback(priority, callback) {
  const currentTime = getCurrentTime();
  const timeout = getTimeout(priority);
  const expirationTime = currentTime + timeout;
  
  const task = { callback, expirationTime, priority };
  taskQueue.push(task);
  taskQueue.sort((a, b) => a.expirationTime - b.expirationTime);
  
  requestHostCallback(workLoop);
}

function workLoop() {
  while (currentTask && !shouldYield()) {
    performWork(currentTask);
    currentTask = getNextTask();
  }
  
  if (currentTask) {
    // 还有工作，继续调度
    return true;
  }
}
```

### 时间切片

```javascript
function timeSlicedWork() {
  const channel = new MessageChannel();
  
  channel.port1.onmessage = () => {
    const deadline = performance.now() + 5; // 5ms 时间片
    
    while (hasWork() && performance.now() < deadline) {
      doWork();
    }
    
    if (hasWork()) {
      channel.port2.postMessage(null); // 继续
    }
  };
  
  channel.port2.postMessage(null); // 开始
}
```

## 🎓 学习建议

1. **按顺序学习**：建议按照导航栏的顺序（从左到右）学习各个演示
2. **动手实践**：每个演示都有交互按钮，多点击尝试，观察日志输出
3. **对比观察**：特别注意"阻塞 vs 非阻塞"、"优先级 vs FIFO"的对比
4. **阅读代码**：演示中的代码块都是可以运行的，建议仔细阅读
5. **总结思考**：学习完所有演示后，回到首页重新阅读核心问题解答

## 🔧 技术栈

- **React 18**：使用最新的 Concurrent Features
- **TypeScript**：类型安全的开发体验
- **React Router**：单页应用路由
- **CSS3**：现代化的 UI 设计

## 📖 相关资源

- [React Fiber Architecture](https://github.com/acdlite/react-fiber-architecture)
- [React Scheduler Package](https://github.com/facebook/react/tree/main/packages/scheduler)
- [Lin Clark - A Cartoon Intro to Fiber](https://www.youtube.com/watch?v=ZCuYPiUIONs)
- [浏览器事件循环规范](https://html.spec.whatwg.org/multipage/webappapis.html#event-loops)
- [React 18 新特性](https://react.dev/blog/2022/03/29/react-v18)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

MIT License

## 💬 常见问题

### Q: 为什么不直接用 requestIdleCallback？

A: requestIdleCallback 只在浏览器空闲时执行，可能等待时间太长。React 需要更精确的控制，所以使用 MessageChannel（无延迟）或 setTimeout（降级方案）。

### Q: Fiber 和 Scheduler 是什么关系？

A: Fiber 是 React 的协调引擎，负责构建虚拟 DOM 树和 diff。Scheduler 是独立的调度库，负责任务优先级管理。Fiber 使用 Scheduler 来调度渲染任务。

### Q: 时间切片会不会让总时间变长？

A: 是的，总时间会稍微变长（因为需要调度开销）。但这是值得的，因为 UI 保持了响应性。宁可慢一点，也不能卡住。

### Q: 为什么是 5ms 时间片？

A: 这是一个经验值。60fps 的情况下，每帧约 16.6ms。如果 React 占用 5ms，还剩 11.6ms 给浏览器做其他事情（处理输入、渲染等），这样可以保持流畅。

---

**Happy Learning! 🎉**

如果这个项目对你有帮助，请给个 Star ⭐️
